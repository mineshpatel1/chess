[build-system]
requires = ['maturin>=1.0,<2.0']
build-backend = 'maturin'

[project]
name = 'zero-rs'
description = 'The Rust Connect 4 self-play engine for ai/zero/.'
requires-python = '>=3.8'
dynamic = ['version']

[tool.maturin]
module-name = 'zero_rs'
